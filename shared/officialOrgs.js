/**
 * Canonical names for Field's official organizations.
 * See docs/official-orgs.md.
 */

/** @type {{ slug: string, displayName: string }} */
export const SANDBOCKS = Object.freeze({
	slug: 'sandbocks',
	displayName: 'Sandbocks',
});

/** @type {{ slug: string, displayName: string }} */
export const ALPHA_INDUSTRIES = Object.freeze({
	slug: 'alpha-industries',
	displayName: 'Alpha Industries',
});
