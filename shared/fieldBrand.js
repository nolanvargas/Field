/**
 * Field product mark and default tenant accent (Sandbocks / dev reset).
 * Logo SVGs mirror FIELD_BRAND_GRADIENT_*; org UI uses a single accent hex.
 */

/** Caramel — golden top, toffee brown bottom (vertical mark gradient). */
export const FIELD_BRAND_GRADIENT_TOP = '#FBBF24';
export const FIELD_BRAND_GRADIENT_BOTTOM = '#92400E';

/** Default org accent; shades derive via orgAccent. */
export const FIELD_BRAND_ACCENT = '#B45309';
