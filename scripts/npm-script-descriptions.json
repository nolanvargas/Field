{
	"dev": "Start API (:3000) and Vite (:5173)",
	"dev:stop": "Free ports 3000 and 5173",
	"build": "Typecheck and production Vite build",
	"lint": "Run oxlint",
	"test": "Run Vitest once",
	"test:watch": "Run Vitest in watch mode",
	"adb:virtual": "Android emulator live reload via 10.0.2.2",
	"adb:physical": "Physical Android live reload via LAN IP",
	"cap:live": "Capacitor live reload (pass platform, e.g. ios)",
	"cap:sync": "Build web app and cap sync to native projects",
	"cap:android": "Sync and open Android Studio",
	"cap:ios": "Sync and open Xcode",
	"android:keystore": "Create release keystore",
	"apk:serve": "Build and serve debug APK over LAN",
	"db:schema": "Apply PostgreSQL migrations",
	"db:import-contacts": "Import contacts from CSV",
	"db:import-addresses": "Import addresses from CSV",
	"db:geocode-addresses": "Geocode address records",
	"db:import-users": "Import users from CSV",
	"db:seed-dev-data": "Seed dev org, users, addresses, and contacts",
	"db:seed-dev-tasks": "Seed sample tasks",
	"db:seed": "Run both dev seed scripts",
	"db:reset": "Reset dev database (destructive)",
	"db:reset-org-config": "Reset org settings to defaults",
	"db:wipe-tasks": "Delete all tasks (destructive)",
	"db:purge-cancelled": "Purge cancelled tasks past retention",
	"email:test": "Send test email via console or SES",
	"import:sheet-spec": "Print Google Sheets import column spec"
}
